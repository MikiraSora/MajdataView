name: Build MajdataEditView
run-name: Build MajdataEditView (${{ github.ref_name }})

on:
  push:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: majdataeditview-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-unity:
    name: Build MajdataView Windows x64
    runs-on: ubuntu-latest

    steps:
      - name: Checkout MajdataView
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true
          submodules: recursive

      - name: Cache Unity Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-StandaloneWindows64-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-StandaloneWindows64-
            Library-

      - name: Build MajdataView Windows x64 player
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          unityVersion: auto
          targetPlatform: StandaloneWindows64
          versioning: Semantic
          buildName: MajdataView
          buildsPath: build

      - name: Upload MajdataView build
        uses: actions/upload-artifact@v4
        with:
          name: MajdataView-StandaloneWindows64
          path: build
          if-no-files-found: error
          retention-days: 14

  build-editor:
    name: Build MajdataEdit Windows x64
    runs-on: windows-latest

    steps:
      - name: Checkout MajdataEdit
        uses: actions/checkout@v4
        with:
          repository: MikiraSora/MajdataEdit
          fetch-depth: 0
          submodules: recursive
          path: MajdataEdit

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 6.0.x

      - name: Restore MajdataEdit
        run: dotnet restore
        working-directory: MajdataEdit

      - name: Publish MajdataEdit
        run: dotnet publish -c Release -r win-x64 --no-self-contained
        working-directory: MajdataEdit

      - name: Upload MajdataEdit build
        uses: actions/upload-artifact@v4
        with:
          name: MajdataEdit-win-x64
          path: MajdataEdit/bin/Release/net6.0-windows/win-x64/publish
          if-no-files-found: error
          retention-days: 14

  package-pages:
    name: Package and Deploy Downloads
    if: github.ref_name == github.event.repository.default_branch
    needs:
      - build-unity
      - build-editor
    runs-on: ubuntu-latest

    steps:
      - name: Download MajdataView build
        uses: actions/download-artifact@v4
        with:
          name: MajdataView-StandaloneWindows64
          path: package

      - name: Download MajdataEdit build
        uses: actions/download-artifact@v4
        with:
          name: MajdataEdit-win-x64
          path: package/MajdataEdit

      - name: Build download page
        shell: bash
        run: |
          python3 <<'PY'
          from datetime import datetime, timezone
          from html import escape
          from pathlib import Path
          from zipfile import ZIP_DEFLATED, ZipFile
          import os

          root = Path.cwd()
          package_dir = root / "package"
          site_dir = root / "site"
          downloads_dir = site_dir / "downloads"
          downloads_dir.mkdir(parents=True, exist_ok=True)

          zip_name = "MajdataEditView.zip"
          zip_path = downloads_dir / zip_name

          with ZipFile(zip_path, "w", ZIP_DEFLATED) as archive:
              for file_path in sorted(package_dir.rglob("*")):
                  if file_path.is_file():
                      archive.write(file_path, file_path.relative_to(package_dir))

          build_time = datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M:%S UTC")
          commit_sha = os.environ["GITHUB_SHA"]
          ref_name = os.environ["GITHUB_REF_NAME"]
          repo = os.environ["GITHUB_REPOSITORY"]

          (site_dir / "index.html").write_text(f"""<!doctype html>
          <html lang="zh-CN">
          <head>
            <meta charset="utf-8">
            <meta name="viewport" content="width=device-width, initial-scale=1">
            <title>MajdataEditView Downloads</title>
            <style>
              :root {{
                color-scheme: light dark;
                font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
              }}
              body {{
                margin: 0;
                min-height: 100vh;
                display: grid;
                place-items: center;
                background: #101820;
                color: #f7fafc;
              }}
              main {{
                width: min(720px, calc(100vw - 32px));
                padding: 32px;
                border: 1px solid #2d3748;
                border-radius: 8px;
                background: #17212b;
                box-sizing: border-box;
              }}
              h1 {{
                margin: 0 0 12px;
                font-size: 28px;
                font-weight: 700;
              }}
              p {{
                color: #cbd5e0;
                line-height: 1.6;
              }}
              a.button {{
                display: inline-block;
                margin: 12px 12px 12px 0;
                padding: 12px 16px;
                border-radius: 6px;
                background: #2b6cb0;
                color: white;
                text-decoration: none;
                font-weight: 600;
              }}
              code {{
                color: #bee3f8;
              }}
            </style>
          </head>
          <body>
            <main>
              <h1>MajdataEditView Downloads</h1>
              <p>这里提供由 GitHub Actions 自动构建的 MajdataView + MajdataEdit Windows x64 合并包。</p>
              <a class="button" href="downloads/{escape(zip_name)}">下载 MajdataEditView</a>
              <p>仓库：<code>{escape(repo)}</code></p>
              <p>分支：<code>{escape(ref_name)}</code></p>
              <p>Commit：<code>{escape(commit_sha)}</code></p>
              <p>构建时间：<code>{escape(build_time)}</code></p>
            </main>
          </body>
          </html>
          """, encoding="utf-8")
          PY

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy-pages:
    name: Deploy GitHub Pages
    if: github.ref_name == github.event.repository.default_branch
    needs: package-pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy download page
        id: deployment
        uses: actions/deploy-pages@v4
